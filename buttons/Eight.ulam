element Eight : Button{
  ID produce(SiteNum slot){
    ID child = (ID) r.create(id.maxof);
    Data d;
    d.value = 8;
    d.parent = id;
    d.child = child;
    ew[slot] = d;

    return child;
  }
  Atom me(){
    return self.atomof;
  }
  
}